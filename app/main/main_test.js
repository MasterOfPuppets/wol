'use strict';

describe('wol.main module', function() {

  beforeEach(module('wol.main'));

  describe('main controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('MainCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});
